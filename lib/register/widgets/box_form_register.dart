import 'package:flutter/material.dart';

class BoxFormRegister extends StatelessWidget {
  const BoxFormRegister({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
